stages:
  - build
  - test
  - contract
  - package

variables:
  MAVEN_OPTS: "-Dmaven.repo.local=$CI_PROJECT_DIR/.m2/repository"
  MAVEN_CLI_OPTS: "-B -ntp"
  DOCKER_HOST: tcp://docker:2375
  DOCKER_TLS_CERTDIR: ""
  TESTCONTAINERS_HOST_OVERRIDE: docker

.maven_cache:
  cache:
    key: maven-repo
    paths:
      - .m2/repository

.maven_job:
  extends: .maven_cache
  image: maven:3.9-eclipse-temurin-24

.dind_job:
  extends: .maven_cache
  image: maven:3.9-eclipse-temurin-24
  services:
    - docker:24-dind

build-microservice:
  extends: .maven_job
  stage: build
  script:
    - mvn $MAVEN_CLI_OPTS -DskipTests package
  artifacts:
    paths:
      - target/*.jar
    expire_in: 1 hour

build-client:
  extends: .maven_job
  stage: build
  script:
    - mvn $MAVEN_CLI_OPTS -f client/pom.xml -DskipTests package
  artifacts:
    paths:
      - client/target/*.jar
    expire_in: 1 hour

unit-test-microservice:
  extends: .maven_job
  stage: test
  needs: ["build-microservice"]
  script:
    - mvn $MAVEN_CLI_OPTS test -Dtest=DefaultTimeServiceTest,TimeControllerTest
  artifacts:
    when: always
    reports:
      junit: target/surefire-reports/TEST-*.xml

unit-test-client:
  extends: .maven_job
  stage: test
  needs: ["build-client"]
  script:
    - mvn $MAVEN_CLI_OPTS -f client/pom.xml test -Dtest=ClientControllerTest
  artifacts:
    when: always
    reports:
      junit: client/target/surefire-reports/TEST-*.xml

integration-test-microservice:
  extends: .dind_job
  stage: test
  needs: ["build-microservice"]
  script:
    - mvn $MAVEN_CLI_OPTS test -Dtest='*IntegrationTest,*RestAssuredTest,*DumpTest'
  artifacts:
    when: always
    reports:
      junit: target/surefire-reports/TEST-*.xml

pact-consumer:
  extends: .maven_job
  stage: contract
  needs: ["build-client"]
  script:
    - mvn $MAVEN_CLI_OPTS -f client/pom.xml test -Dtest=TimeClientPactTest
  artifacts:
    when: always
    paths:
      - pacts/*.json
    reports:
      junit: client/target/surefire-reports/TEST-*.xml
    expire_in: 1 week

pact-provider-verify:
  extends: .dind_job
  stage: contract
  needs: ["build-microservice", "pact-consumer"]
  script:
    - mvn $MAVEN_CLI_OPTS test -Dtest=TimeApiPactVerificationTest
  artifacts:
    when: always
    reports:
      junit: target/surefire-reports/TEST-*.xml

docker-build-microservice:
  stage: package
  image: docker:24
  services:
    - docker:24-dind
  needs: ["unit-test-microservice", "integration-test-microservice", "pact-provider-verify"]
  script:
    - docker build -t microservice:$CI_COMMIT_SHORT_SHA .

docker-build-client:
  stage: package
  image: docker:24
  services:
    - docker:24-dind
  needs: ["unit-test-client", "pact-consumer"]
  script:
    - docker build -t time-client:$CI_COMMIT_SHORT_SHA ./client
